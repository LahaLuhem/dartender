name: dart_package
description: A pure Dart package that dartender's self-test runs ci.yml against.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0

dev_dependencies:
  dependency_validator: ^5.1.0
  test: ^1.32.0
